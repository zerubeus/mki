---
// src/pages/en/history.astro
import Layout from '../../layouts/Layout.astro';
import SeerahJourney from '../../components/SeerahJourney.tsx';
import type { Locale } from '../../i18n/translations';
import { getTranslations } from '../../i18n/utils';

const currentLocale = (Astro.currentLocale as Locale) || 'en';
const t = getTranslations(currentLocale);
---

<Layout>
  <head>
    {/* Leaflet CSS is in the main Layout.astro, no need to repeat here */}
    {
      /* Ensure Leaflet JS is loaded if InteractiveMap.tsx uses window.L (e.g., in Layout.astro) */
    }

    {
      /* Tailwind (ensure this is set up in your astro.config.mjs and global CSS) */
    }

    {/* Font for ﷺ symbol if needed and not global */}
    <link
      href="https://fonts.googleapis.com/css2?family=Amiri+Quran&display=swap"
      rel="stylesheet"
    />
  </head>
  <SeerahJourney client:load t={t} locale={currentLocale} />
  {/* Pass t object as a prop */}
</Layout>
